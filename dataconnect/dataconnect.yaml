specVersion: "v1beta"
serviceId: "fintracker"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fintracker-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
